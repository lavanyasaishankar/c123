#include <stdio.h>

int main() 
{
	int a[i],i,max;
	printf("\n enter the ten numbers");
	sacnf("%d",a[i]);
	for(i=0;i>a[i];i++)
	{
		max=a[0];
		if(a[i]>max)
		{
				max=a[i];
			printf("the greatest among the ten numbers is %d",max);
		}
	}
	
	return 0;
}
