#include <stdio.h>
#include<string.h>
int main(void)
{
	char l[50];
	int i,count=0;
	gets(a);
	
	for(i=0;l[i]!='\0';i++)
	{
		if(l[i]>='0' &&  l[i]<='9')
		count++;
	}
	printf("%d",count);
	return 0;
}
